# OpenClaw Root Files

File root penting yang sering jadi referensi:

- `README.md` — orientasi project
- `AGENTS.md` / `CLAUDE.md` — aturan kerja agent
- `SECURITY.md` — trust model dan disclosure
- `VISION.md` — arah produk
- `package.json` — scripts dan workspace behavior
- `pnpm-workspace.yaml` — monorepo map
- `Dockerfile`, `docker-compose.yml`, deploy configs

## Relevansi ke AgentAI01

- policy files dipakai sebagai benchmark disiplin engineering
- root workspace file dipakai untuk memahami scale dan ownership model
- tidak semua root behavior OpenClaw perlu diadopsi ke repo ini
